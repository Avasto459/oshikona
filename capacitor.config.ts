import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.oshiqona.client',
  appName: 'npx cap add android',
  webDir: 'dist'
};

export default config;
